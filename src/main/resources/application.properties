# SQLite DataSource
spring.datasource.url=jdbc:sqlite:finance.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect

# JPA properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server configuration
server.port=8080

# Actuator configuration (for health checks)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.livenessState.enabled=true
management.health.readinessState.enabled=true

# Session configuration
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax

# Logging configuration
logging.level.root=INFO
logging.level.com.example.finance=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
logging.file.name=/logs/app.log
logging.file.max-size=10MB
logging.file.max-history=30
logging.file.total-size-cap=300MB

# News API Configuration
# 新闻API配置 - 请选择一个API并配置密钥
# 1. NewsAPI (https://newsapi.org/) - 免费计划每天100次请求
# 2. 天行数据 (https://www.tianapi.com/) - 免费计划每天100次请求
# 3. 聚合数据 (https://www.juhe.cn/) - 免费计划有限额

# NewsAPI配置（推荐用于国际新闻和中文新闻）
news.api.source=newsapi
news.api.newsapi.key=431a2ae998bb4d8ebef71660d9023d5d
news.api.newsapi.country=us
news.api.newsapi.pageSize=20

# 天行数据配置（备选，中文新闻）
news.api.tianapi.key=YOUR_TIANAPI_KEY_HERE

# 缓存配置
news.cache.duration.minutes=30
