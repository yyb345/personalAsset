# SQLite DataSource
spring.datasource.url=jdbc:sqlite:finance.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect

# JPA properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Server configuration
server.port=8081

# Actuator configuration (for health checks)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.livenessState.enabled=true
management.health.readinessState.enabled=true

# Session configuration
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax

# ==================== OAuth2 Configuration ====================
# Google OAuth2 (需要在 Google Cloud Console 创建应用获取)
# 1. 访问 https://console.cloud.google.com/
# 2. 创建项目 -> APIs & Services -> Credentials -> Create OAuth Client ID
# 3. 配置 Authorized redirect URI: http://localhost:8081/login/oauth2/code/google
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:51855213774-d95hqp0ffnt5liet5r6eqpg70ggqdk84.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=email,profile

# GitHub OAuth2 (可选，需要在 GitHub Developer Settings 创建应用获取)
# spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID:your-github-client-id}
# spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET:your-github-client-secret}
# spring.security.oauth2.client.registration.github.scope=read:user,user:email

# Async thread pool for downloads
spring.task.execution.pool.core-size=3
spring.task.execution.pool.max-size=5
spring.task.execution.pool.queue-capacity=10
spring.task.execution.thread-name-prefix=download-

# Logging configuration
logging.level.root=INFO
logging.level.com.example.finance=INFO
logging.level.org.hibernate.SQL=ERROR
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=ERROR
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
logging.file.name=/logs/app.log
logging.file.max-size=10MB
logging.file.max-history=30
logging.file.total-size-cap=300MB
